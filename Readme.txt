After cloning this repository: 
- npm install

Add your Google API Key in app.json file.

To build the project and generate APKs (android builds) and IPAs (ios builds):
- npm install -g eas-cli
- eas build -p android --profile standalone


